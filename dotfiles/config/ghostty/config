# Ghostty Configuration

# Enable URL clicking (should work by default in Ghostty)
link-url = true

# Mouse settings
mouse-hide-while-typing = false

# Shell integration - use 'detect' instead of 'true'
shell-integration = detect
shell-integration-features = cursor,sudo,title

# Font settings
font-family = SF Mono
font-size = 14

# Performance
macos-non-native-fullscreen = false