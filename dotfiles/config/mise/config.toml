[tools]
# Python is intentionally managed with uv; use mise for other runtimes.
node = "20.20.0"
usage = "latest"
rust = "1.84.0"
ruby = "3.4.2"

[settings]
experimental = true
idiomatic_version_file_enable_tools = []
